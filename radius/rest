rest {
    connect_uri = "http://api:8000"

    authenticate {
        uri = "${..connect_uri}/auth"
        method = 'post'
        body = 'json'
        tls = ${..tls}
    }

    authorize {
        uri = "${..connect_uri}/authorize"
        method = 'post'
        body = 'json'
        tls = ${..tls}
    }

    accounting {
        uri = "${..connect_uri}/accounting"
        method = 'post'
        body = 'json'
        tls = ${..tls}
    }
}
